# FastAPI & Server
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# ── Google AI ──────────────────────────────────────────────────────────────
# Gemini via google-genai SDK (direct API — works without GCP project)
google-genai==1.3.0

# Google Auth (shared by all GCP SDKs)
google-auth>=2.29.0

# ── Google Cloud Services (optional — graceful fallback if unavailable) ────
# Install only what's needed; each service has a local fallback in code.
# Comment out any you don't need to speed up Render builds.

# Vertex AI (enterprise Gemini — only needed if GOOGLE_CLOUD_PROJECT is set)
google-cloud-aiplatform>=1.60.0

# Cloud Vision API (OCR for images/scanned PDFs — falls back to Tesseract)
google-cloud-vision>=3.7.0

# Cloud Storage (file uploads — falls back to /tmp)
google-cloud-storage>=2.16.0

# Cloud Firestore (document DB — falls back to MongoDB then in-memory)
google-cloud-firestore>=2.16.0

# Cloud Natural Language API
google-cloud-language>=2.13.0

# ── Document Processing ───────────────────────────────────────────────────
PyPDF2==3.0.1
pdfplumber==0.11.4
python-docx==1.1.2
pytesseract==0.3.13          # Tesseract OCR (needs tesseract binary — optional)
openpyxl==3.1.5
Pillow==12.2.0
# python-magic-bin: self-contained libmagic (no system libmagic needed — Render safe)
python-magic-bin==0.4.14; sys_platform == "win32"
python-magic==0.4.27; sys_platform != "win32"

# ── RTF support ──────────────────────────────────────────────────────────
striprtf==0.0.26

# ── Database ──────────────────────────────────────────────────────────────
motor==3.7.0
pymongo==4.10.1

# ── NLP & Text Processing ────────────────────────────────────────────────
tiktoken==0.8.0
httpx==0.28.1

# ── HTTP & Utils ─────────────────────────────────────────────────────────
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.1
aiofiles==24.1.0

# ── Testing ───────────────────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.25.0

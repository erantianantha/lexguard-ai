# FastAPI & Server
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# ── Google Cloud Services ──────────────────────────────────────────────────
# Gemini via google-genai SDK (direct API)
google-genai==1.3.0

# Vertex AI (enterprise Gemini + Document AI + Vision)
google-cloud-aiplatform>=1.60.0
vertexai>=1.60.0

# Cloud Vision API (OCR for images & scanned PDFs)
google-cloud-vision>=3.7.0

# Cloud Storage (file uploads)
google-cloud-storage>=2.16.0

# Cloud Firestore (document DB)
google-cloud-firestore>=2.16.0

# Cloud Natural Language API (entity extraction supplement)
google-cloud-language>=2.13.0

# Google Auth
google-auth>=2.29.0

# ── Document Processing ───────────────────────────────────────────────────
PyPDF2==3.0.1
pdfplumber==0.11.4
python-docx==1.1.2
pytesseract==0.3.13          # Tesseract OCR fallback (when Vision API unavailable)
openpyxl==3.1.5
Pillow==12.2.0
python-magic==0.4.27         # MIME-type detection from file bytes

# ── Database ──────────────────────────────────────────────────────────────
motor==3.7.0
pymongo==4.10.1

# ── NLP & Text Processing ────────────────────────────────────────────────
tiktoken==0.8.0
httpx==0.28.1

# ── HTTP & Utils ─────────────────────────────────────────────────────────
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.1
aiofiles==24.1.0

# ── Testing ───────────────────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.25.0
